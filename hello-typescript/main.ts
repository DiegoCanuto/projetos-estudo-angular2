var minhaVar = 'minha variavel';

function minhaFunciz(x,y) {
    return x+y;
}


// ES 6 ou ES 2015
let numez = 1;
const pizi =2;

var numeros = [1,2,3];
numeros.map(function(valor) {
    return valor *2;
});

numeros.map( valor => valor * 2);