var minhaVar = 'minha variavel';
function minhaFunci(x, y) {
    return x + y;
}
// ES 6 ou ES 2015
var nume = 1;
var pii = 2;
var numeros = [1, 2, 3];
numeros.map(function (valor) {
    return valor * 2;
});
numeros.map(function (valor) { return valor * 2; });
