import { FundoAmarelaDirective } from './fundo-amarela.directive';

describe('FundoAmarelaDirective', () => {
  it('should create an instance', () => {
    const directive = new FundoAmarelaDirective();
    expect(directive).toBeTruthy();
  });
});
